volumes/
laravel-backend/

# Environment files (keep .env.example)
.env
.env.production
.env.local
.env.*.local
*.env

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude
.claude/
.playwright-mcp/

# Provisioning scratch. setup_aws/ has held downloaded IAM access-key CSVs, so
# leaving it unignored means a bulk `git add -A` commits live credentials.
setup_aws/
setup_oracle/
docker-compose.local-db.yml
